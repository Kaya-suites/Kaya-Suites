{
  "name": "@kaya/markdown-model",
  "version": "0.1.0",
  "private": true,
  "description": "Parse, serialize, and types for Kaya's Markdown block model (Apache 2.0)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
